#include <stdio.h>

int main() {
    int n = 10, fib[20], sum = 0;
    fib[0] = 0; fib[1] = 1;
    for (int i = 2; i < n; i++) fib[i] = fib[i - 1] + fib[i - 2];
    for (int i = 0; i < n; i += 2) sum += fib[i];
    printf("Sum at even indexes = %d\n", sum);
    return 0;
}
